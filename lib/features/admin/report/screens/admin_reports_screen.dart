import 'package:flutter/material.dart';

class AdminReportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Welcome to the Admin Report!',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}