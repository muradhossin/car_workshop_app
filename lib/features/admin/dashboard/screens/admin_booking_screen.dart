import 'package:flutter/material.dart';

class AdminBookingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Welcome to the Admin Booking!',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}