import 'package:flutter/material.dart';

class AdminMechanicScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Welcome to the Admin Mechanics!',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}